import { CircularProgress } from "@mui/material";

const ViewInformationLoader = () => {
    return (
        <div className="loaadinger loaderrr-2">
            <CircularProgress color="error" />
        </div>
    );
};

export default ViewInformationLoader